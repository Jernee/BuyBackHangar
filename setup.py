from setuptools import setup, find_packages

setup(
    name='buyback-hangar',
    version='0.1.0',
    description='Alliance Auth plugin to display corp hangar contents',
    author='Your Name',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
    zip_safe=False,
)
